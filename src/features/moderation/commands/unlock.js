const { SlashCommandBuilder, PermissionFlagsBits }=require("discord.js"); const Categories=require("../../../constants/categories");
module.exports={data:new SlashCommandBuilder().setName("unlock").setDescription("Unlock a text channel.").setDefaultMemberPermissions(PermissionFlagsBits.ManageChannels).addChannelOption(o=>o.setName("channel").setDescription("Channel to unlock; defaults to current channel.")).addStringOption(o=>o.setName("reason").setDescription("Reason for unlocking the channel.").setMaxLength(500)),category:Categories.MODERATION,description:"Restores @everyone's default send-message permission in a selected channel.",usage:"/unlock [channel] [reason]",permissions:["Manage Channels"],cooldown:5,async execute(i){const c=i.options.getChannel("channel")||i.channel;if(!c?.permissionOverwrites)return i.reply({content:"❌ That channel cannot be unlocked.",ephemeral:true});await c.permissionOverwrites.edit(i.guild.roles.everyone,{SendMessages:null},{reason:i.options.getString("reason")||`Unlocked by ${i.user.tag}`});await i.reply({content:`🔓 ${c} has been unlocked.`,ephemeral:true});}};
