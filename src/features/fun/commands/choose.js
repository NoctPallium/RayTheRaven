const { SlashCommandBuilder, EmbedBuilder }=require("discord.js"); const Categories=require("../../../constants/categories");
module.exports={data:new SlashCommandBuilder().setName("choose").setDescription("Let Ray choose between options.").addStringOption(o=>o.setName("options").setDescription("Separate choices with commas.").setRequired(true).setMaxLength(1000)),category:Categories.FUN,description:"Randomly chooses from a comma-separated list of options.",usage:"/choose options: pizza, burgers, tacos",permissions:[],cooldown:2,async execute(i){const options=i.options.getString("options").split(",").map(v=>v.trim()).filter(Boolean);if(options.length<2)return i.reply({content:"❌ Give me at least two choices separated by commas.",ephemeral:true});const choice=options[Math.floor(Math.random()*options.length)];await i.reply({embeds:[new EmbedBuilder().setColor("#7C3AED").setTitle("🐦‍⬛ Ray Chooses").setDescription(`I choose **${choice}**.`)]});}};
